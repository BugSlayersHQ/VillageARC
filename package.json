{
  "name": "villageARC",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "24.20.0"
  },
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "typecheck": "pnpm --filter frontend exec tsc --noEmit && pnpm --filter backend typecheck && pnpm --filter worker typecheck",
    "build": "pnpm --filter frontend build && pnpm --filter backend build && pnpm --filter worker build",
    "prepare": "husky"
  },
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "husky": "9.1.7",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.0"
  }
}
